const colors = require("tailwindcss/colors");

module.exports = {
    purge: [],
    darkMode: false, // or 'media' or 'class'
    theme: {
        colors: {
            "indigo-lighter": "#b3bcf5",
            "indigo-dark": "#202e78",
        },

        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};